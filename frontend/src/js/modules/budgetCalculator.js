// TODO: Calculateur de Budget
// Ce module contiendra la logique pour estimer les coûts (scolarité, vie, installation).

export const BudgetCalculator = {
    // calculateTotal: (params) => { ... },
    // getTuitionFees: (level, program) => { ... }
};
