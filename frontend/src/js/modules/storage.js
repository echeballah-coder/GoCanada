// TODO: Gestion du LocalStorage
// Ce module servira à sauvegarder l'état des checklists et les simulations de budget.

export const Storage = {
    // save: (key, data) => { ... },
    // load: (key) => { ... },
    // clear: (key) => { ... }
};
